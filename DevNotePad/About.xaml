<Window x:Class="DevNotePad.About"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DevNotePad"
        mc:Ignorable="d"
        Title="About" 
        MinHeight="200" 
        MinWidth="300" 
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight"
        WindowStyle="ToolWindow"
        Closing="Window_Closing"
        FocusManager.FocusedElement="{Binding ElementName=aboutText}"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Name="aboutText">
            DevNotePad
        </TextBlock>

        <Button Width="100" Margin="0 10 0 10" Grid.Row="1" IsDefault="True" Click="Button_Click">
            OK
        </Button>
        
    </Grid>
</Window>
